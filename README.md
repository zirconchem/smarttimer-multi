# Smart Timer Remote

Android app for controlling the ESP32 Smart Timer via MQTT.

The APK is built automatically on every push. Download the latest
from the **Actions** tab → click the latest run → **Artifacts**.